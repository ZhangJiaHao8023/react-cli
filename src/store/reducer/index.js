const initialState = {
    list: []
}

const reducer = (state = initialState, { type, value }) => {
    switch (type) {
        // case 'INIT':
        //     return {
        //         ...state,
        //         list: value,
        //     }
    
    }
    return state
}
export default reducer
